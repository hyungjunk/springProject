<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="adminMapper">

	<insert id="create">
		INSERT INTO movie_contents (name, contents, runtime, imgpath) VALUES (#{name}, #{contents}, #{runtime}, #{imgpath}) 
	</insert>
	
	<select id="read" resultType="com.jun.domain.MovBoardVO">
		SELECT mid, name, contents, runtime, viewcnt, regdate, moddate, imgpath FROM movie_contents where mid = #{mid}
	</select>
	
	<update id="update">
		UPDATE movie_contents SET name = #{name}, contents = #{contents}, runtime = #{runtime}, imgpath = #{imgpath} where mid=#{mid}
	</update>
	
	<select id="readAll" resultType="com.jun.domain.MovBoardVO">
		SELECT * FROM movie_contents order by regdate desc LIMIT #{pageStart}, #{perPageNum}
	</select>
	
	<select id="totalCount" resultType="int">
		SELECT count(*) FROM movie_contents
	</select>
	
	<delete id="delete">
		DELETE from movie_contents where mid = #{mid}
	</delete>
	
</mapper>